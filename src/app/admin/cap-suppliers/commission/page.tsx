'use client';

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function CommissionPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Commission</CardTitle>
                <CardDescription>Manage commissions.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Commission management functionality will be built here.</p>
            </CardContent>
        </Card>
    );
}
