rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open read and write access for all portal collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}